interface:
  display_name: "ThinkWeave Ingest"
  short_description: "Universal input router — classifies input shape (URL / file / text / structured-"
  default_prompt: "Use $thinkweave-ingest to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
