interface:
  display_name: "ThinkWeave Judge Prediction"
  short_description: "Evaluate one or more decisions' predicted_outcome against current vault + filesy"
  default_prompt: "Use $thinkweave-judge-prediction to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
