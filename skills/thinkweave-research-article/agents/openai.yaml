interface:
  display_name: "ThinkWeave Research Article"
  short_description: "Fetch a web article, extract argument + claims + evidence, write it as a `source"
  default_prompt: "Use $thinkweave-research-article to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
