interface:
  display_name: "ThinkWeave Drain"
  short_description: "Drain a per-source-type acquisition queue. Source-type-agnostic — config in `vau"
  default_prompt: "Use $thinkweave-drain to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
