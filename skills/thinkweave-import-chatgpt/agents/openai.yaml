interface:
  display_name: "ThinkWeave Import Chatgpt"
  short_description: "Inline ChatGPT-export import — walk conversations.json, summarize each thread vi"
  default_prompt: "Use $thinkweave-import-chatgpt to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
