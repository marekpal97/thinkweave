interface:
  display_name: "ThinkWeave Capture"
  short_description: "Save reusable knowledge into ThinkWeave"
  default_prompt: "Use $thinkweave-capture to preserve this insight in ThinkWeave."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"