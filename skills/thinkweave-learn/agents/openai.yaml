interface:
  display_name: "ThinkWeave Learn"
  short_description: "Vault-grounded tutor — `/learn <topic>`. Teach-then-test dialogue; the vault is"
  default_prompt: "Use $thinkweave-learn to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
