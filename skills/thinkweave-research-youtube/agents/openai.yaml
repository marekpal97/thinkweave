interface:
  display_name: "ThinkWeave Research Youtube"
  short_description: "Resolve a single pasted YouTube URL into a queue item and drain one `research-yo"
  default_prompt: "Use $thinkweave-research-youtube to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
