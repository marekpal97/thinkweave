interface:
  display_name: "ThinkWeave Research"
  short_description: "Router skill — classifies URLs and dispatches to research-paper / research-repo"
  default_prompt: "Use $thinkweave-research to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
