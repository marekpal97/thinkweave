interface:
  display_name: "ThinkWeave Research"
  short_description: "Research a URL and preserve the result"
  default_prompt: "Use $thinkweave-research to investigate this URL and save the useful result."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"