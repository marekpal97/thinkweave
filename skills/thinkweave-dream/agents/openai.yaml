interface:
  display_name: "ThinkWeave Dream"
  short_description: "Periodic dream cycle — two-phase subagent orchestrator. Phase 1 fans out 5 synth"
  default_prompt: "Use $thinkweave-dream to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
