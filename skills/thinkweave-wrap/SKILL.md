---
name: thinkweave-wrap
description: "End-of-session memory extraction. Compose insights/decisions inline, call `weave_extract` once, then `weave wrap-finalize` (deterministic tail). Self-contained; never prompts the user."
---

# Codex projection for `/wrap`

Read the [shared Codex adapter](../../docs/CODEX-SKILL-PROJECTION.md)
and the [canonical ThinkWeave command contract](../../commands/wrap.md) completely, then execute the canonical contract through
that adapter.
