interface:
  display_name: "ThinkWeave Wrap"
  short_description: "Persist session insights and decisions"
  default_prompt: "Use $thinkweave-wrap to extract and finalize this session in ThinkWeave."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"