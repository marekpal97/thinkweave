interface:
  display_name: "ThinkWeave Wrap"
  short_description: "End-of-session memory extraction. Compose insights/decisions inline, call `weave"
  default_prompt: "Use $thinkweave-wrap to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
