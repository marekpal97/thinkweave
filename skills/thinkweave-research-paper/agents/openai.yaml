interface:
  display_name: "ThinkWeave Research Paper"
  short_description: "Fetch a paper (arxiv / openreview / PDF), extract a technical brief, write it as"
  default_prompt: "Use $thinkweave-research-paper to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
