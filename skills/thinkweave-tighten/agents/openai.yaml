interface:
  display_name: "ThinkWeave Tighten"
  short_description: "On-demand ontology-tightening front door for BOTH hub families — review drift-v2"
  default_prompt: "Use $thinkweave-tighten to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
