interface:
  display_name: "ThinkWeave Recall"
  short_description: "Explicit durable-memory recall workflow"
  default_prompt: "Use $thinkweave-recall to recover relevant prior decisions and project context."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
