interface:
  display_name: "ThinkWeave Research Repo"
  short_description: "Fetch a GitHub / GitLab repo, extract architectural summary + key files, write i"
  default_prompt: "Use $thinkweave-research-repo to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
