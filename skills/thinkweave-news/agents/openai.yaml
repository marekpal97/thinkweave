interface:
  display_name: "ThinkWeave News"
  short_description: "One-off URL ingest for news articles, mid-conversation. Dispatches a Sonnet writ"
  default_prompt: "Use $thinkweave-news to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
