interface:
  display_name: "ThinkWeave Youtube"
  short_description: "Thin orchestrator over the YouTube intake rails. Calls the `rss_poll` discover s"
  default_prompt: "Use $thinkweave-youtube to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
