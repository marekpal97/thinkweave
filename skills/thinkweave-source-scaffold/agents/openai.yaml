interface:
  display_name: "ThinkWeave Source Scaffold"
  short_description: "Generative wizard — register a new source type via vault overlay + machine-globa"
  default_prompt: "Use $thinkweave-source-scaffold to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
