interface:
  display_name: "ThinkWeave Source Fit"
  short_description: "Read-only diagnostic — given a free-form description of an input shape, classify"
  default_prompt: "Use $thinkweave-source-fit to run this ThinkWeave workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "thinkweave"
      description: "ThinkWeave durable-memory tools"
