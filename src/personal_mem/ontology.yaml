# personal_mem concept ontology
#
# Domain → [concepts]. Concepts are the typed-edge vocabulary that links
# notes in the knowledge graph; any two notes sharing 2+ concepts auto-link.
#
# This file ships as a minimal seed. It is meant to grow as you use the
# vault — every time you run /mem-resolve-concepts, proposed concepts from
# recent notes get canonicalized and (with your approval) folded in here.
# Over months of use it becomes a faithful map of your actual vocabulary.
#
# For a fuller reference showing how the ontology looks after real-world
# use across ML, AI tooling, finance, and software engineering domains,
# see `ontology.example.yaml` in the same directory.
#
# Rules of thumb:
#   - Concepts must be kebab-case, lowercase.
#   - A concept can appear in multiple domains (cross-listing is fine).
#   - Domains become Obsidian hub pages via `mem concepts hubs`.
#   - Only concepts with vault-wide count >= 5 are worth listing; anything
#     rarer should live as a `proposed_concepts` frontmatter field until it
#     earns its place here.


# Tag vocabulary — the canonical set of tags. Tags are broad categories on
# notes (state, source class, kind of work). They are distinct from
# concepts, which are domain-specific vocabulary forming the typed-edge
# graph. A term should live in one or the other — never both. The linter
# (`mem doctor`) treats this list as canonical and reports tags outside it
# as drift. Categories live in comments alongside each entry.

tag_vocabulary:
  - todo            # workflow: open work item
  - parked          # workflow: deliberately deferred (body explains why)
  - probe           # workflow: user question + discovery — learning artifact
  - theme           # workflow: narrative aggregator (e.g. trade themes)
  - trade           # workflow: trading position decision
  - til             # workflow: today-i-learned
  - research        # source: research backlog item
  - awesome-list    # source: curated-index repo
  - refactor        # meta: structural code change
  - debugging       # meta: investigation / fix
  - performance     # meta: speed / resource concern


swe/python:
  - python
  - pytest
  - typing
  - pathlib

ai/llms:
  - llm
  - prompt-engineering
  - tool-use
  - context-window

ai/tools:
  - claude-code
  - mcp-server
  - obsidian

# Finance / trading vocabulary.
# Concepts here are INVARIANT (stable event types, structures, regimes).
# Named events ("iran-war-2026", "2024-election") belong in theme notes, not
# in the concept ontology — they cite these concepts, they don't extend them.

finance/markets:
  - market-data
  - asset-prices
  - correlation
  - liquidity
  - leverage
  - thematic-investing
  - equity-baskets
  - commodities
  - semiconductors
  - critical-minerals

finance/options:
  - options-strategy
  - volatility-surface
  - options-engine
  - options-trading
  - implied-volatility
  - vega
  - theta
  - iron-condor

finance/quant:
  - factor-model
  - fama-french-factors
  - portfolio-management
  - risk-management
  - reverse-dcf
  - quantitative-analysis

finance/macro:
  - macro-trading
  - inflation
  - interest-rates
  - yield-curve
  - fiscal-deficit
  - dollar-cycle
  - recession

finance/regime:
  - risk-on
  - risk-off
  - reflation
  - stagflation
  - disinflation
  - goldilocks

finance/policy:
  - fomc
  - rate-cut
  - rate-hike
  - quantitative-easing
  - quantitative-tightening

finance/catalysts:
  - election
  - fomc-meeting
  - cpi-release
  - earnings-season
  - geopolitical-shock

finance/geopolitics:
  - geopolitics
  - middle-east-conflict
  - us-china-tensions
  - trade-war
  - sanctions
  - energy-supply-shock

finance/structure:
  - carry-trade
  - pairs-trade
  - vertical-spread
  - bull-call-spread
  - long-vol
  - short-vol
  - tail-hedge
  - mean-reversion
  - momentum
