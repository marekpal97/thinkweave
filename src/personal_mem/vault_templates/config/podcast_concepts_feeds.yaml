# personal_mem podcast-concepts feed registry
#
# Concept-grain podcasts — deep-dives, lecture-style shows, technical
# explainer pods whose value is durable knowledge rather than time-
# anchored signal. Concept hubs handle synthesis; no theme floating.
# Event-grain shows (markets recaps, interview shows with weekly arc)
# go in podcast_events_feeds.yaml instead.
#
# Same shape as podcast_events_feeds.yaml — see that file's header for
# field documentation and how to find an RSS URL.

outlets:
  # ─── Examples (commented out — uncomment + verify URL to subscribe) ────
  #
  # dwarkesh:
  #   name: Dwarkesh Podcast
  #   feeds: [<verify-via-castro-or-itunes-lookup>]
  #   tier: 1
  #   language: en
  #   daily_cap: 1
  #
  # mlst:
  #   name: Machine Learning Street Talk
  #   feeds: [<verify-via-castro-or-itunes-lookup>]
  #   tier: 1
  #   language: en
  #   daily_cap: 1
  #
  # latent-space:
  #   name: Latent Space
  #   feeds: [<verify-via-castro-or-itunes-lookup>]
  #   tier: 1
  #   language: en
  #   daily_cap: 1
