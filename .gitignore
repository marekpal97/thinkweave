# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# pytest
.pytest_cache/

# IDE
.idea/
.vscode/
*.swp

# Environment
.env

# uv
.python-version

# Claude Code local state — but keep the plugin shell (it ships with the repo)
.claude/*
!.claude/plugins/

# MCP config (may contain local paths)
.mcp.json

# Vault data (user-specific)
vault/

# Stashed one-shot migrations (kept locally, not in git)
_old/
