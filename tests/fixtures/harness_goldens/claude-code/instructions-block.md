<!-- thinkweave:start -->
If `weave_*` MCP tools are available, thinkweave (Obsidian-native memory layer) is your durable memory for this session. Prefer `weave_search` / `weave_context` / `weave_graph` over filesystem search, and run `/wrap` before `/clear`.
<!-- thinkweave:end -->
